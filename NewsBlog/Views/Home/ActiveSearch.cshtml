@model IEnumerable<NewsBlog.Models.NewsModel>

<div id="searchresults">
    @if (Model.Count() > 0)
    {
        @foreach (var item in Model)
        {
            <a asp-controller="News" asp-action="ShowNews"
       asp-route-id="@item.Id">@item.NewsTitle</a>
        }
    }
</div>